# models/intermediate/sales_database/int_sales_database__user.yml
version: 2

models:
  - name: int_sales_database__user
    description: '{{ doc("int_sales_database__user") }}'
    columns:
      - name: user_name
        description: "Primary key. Unique identifier of the user."
      - name: user_city
        description: "City of the user."
      - name: total_order
        description: "Number of distinct orders placed by the user."
      - name: total_amount_order
        description: "Total amount spent by the user across all orders."
